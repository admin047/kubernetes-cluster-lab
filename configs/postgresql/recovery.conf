# recovery.conf / standby config (Postgres12+ 使用 standby.signal + postgresql.auto.conf)
# 放置路径：configs/postgresql/recovery.conf
# 仅作示例参考，实际生成会写入到 data_dir/postgresql.auto.conf
primary_conninfo = 'host=192.168.76.4 user=replicator password=repl_pass'
