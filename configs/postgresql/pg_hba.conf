# pg_hba.conf (示例)
# 放置路径：configs/postgresql/pg_hba.conf
# 允许主-从连接与管理连接

# TYPE  DATABASE        USER            ADDRESS                 METHOD
host    replication     replicator      192.168.76.0/24         md5
host    all             all             127.0.0.1/32            md5
host    all             all             ::1/128                 md5
