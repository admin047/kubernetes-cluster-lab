# 常见问题 (FAQ) - Kubernetes 集群实验室

## 📌 目录

1. [集群搭建](#集群搭建)
2. [网络配置](#网络配置)
3. [存储管理](#存储管理)
4. [应用部署](#应用部署)
5. [监控与日志](#监控与日志)
6. [故障排除](#故障排除)

---

## 集群搭建

### Q1: 如何初始化 Kubernetes 集群？

**A:** 参考 `kubectl集群搭建` 文档，主要步骤包括：
1. 安装 kubeadm、kubelet、kubectl
2. 初始化主节点: `kubeadm init`
3. 配置网络插件
4. 加入工作节点

详见: [kubectl集群搭建](./kubectl集群搭建)

---

### Q2: kubeadm init 失败，提示 cgroup 驱动不匹配？

**A:** 这是常见问题。解决方法：

```bash
# 1. 检查当前 cgroup 驱动
docker info | grep "Cgroup Driver"
kubectl get nodes -o jsonpath='{.items[*].status.nodeInfo.containerRuntimeVersion}'

# 2. 修改 Docker cgroup 驱动为 systemd
cat > /etc/docker/daemon.json <<EOF
{
  "exec-opts": ["native.cgroupdriver=systemd"]
}
EOF

# 3. 重启 Docker
sudo systemctl daemon-reload
sudo systemctl restart docker

# 4. 重新初始化集群
kubeadm reset
kubeadm init --pod-network-cidr=10.244.0.0/16
```

---

### Q3: 节点处于 NotReady 状态？

**A:** 这通常是网络插件未安装导致的。

```bash
# 1. 检查节点状态
kubectl get nodes
kubectl describe node <node-name>

# 2. 检查核心 DNS
kubectl get pods -n kube-system | grep coredns

# 3. 安装网络插件 (以 Flannel 为例)
kubectl apply -f https://raw.githubusercontent.com/coreos/flannel/master/Documentation/kube-flannel.yml

# 4. 等待节点就绪
kubectl get nodes -w
```

---

## 网络配置

### Q4: 如何配置 Ingress-Nginx？

**A:** 参考 `Kubernetes Ingress-Nginx` 文档。基本步骤：

```bash
# 1. 安装 Ingress-Nginx
kubectl apply -f https://raw.githubusercontent.com/kubernetes/ingress-nginx/controller-v1.0.0/deploy/static/provider/cloud/deploy.yaml

# 2. 验证安装
kubectl get pods -n ingress-nginx

# 3. 创建 Ingress 资源
kubectl apply -f ingress-example.yaml
```

详见: [Kubernetes Ingress-Nginx](./Kubernetes%20Ingress-Nginx)

---

### Q5: 部署的应用无法通过 Ingress 访问？

**A:** 检查以下几点：

```bash
# 1. 检查 Ingress 状态
kubectl get ingress -A
kubectl describe ingress <ingress-name> -n <namespace>

# 2. 检查后端服务
kubectl get svc -n <namespace>
kubectl get endpoints -n <namespace>

# 3. 检查 Ingress-Nginx 日志
kubectl logs -n ingress-nginx deployment/ingress-nginx-controller

# 4. 常见原因和解决方案
# - Service 不存在: kubectl get svc -n <namespace>
# - 标签选择器不匹配: kubectl get pods -n <namespace> -L app
# - 后端 Pod 未运行: kubectl get pods -n <namespace>
```

---

## 存储管理

### Q6: 如何在 Kubernetes 中使用持久化存储？

**A:** 使用 PVC (PersistentVolumeClaim)。

```yaml
# 1. 创建 StorageClass
apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: fast
provisioner: kubernetes.io/aws-ebs
parameters:
  type: gp2
  iops: "1000"

---
# 2. 创建 PVC
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: my-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast
  resources:
    requests:
      storage: 10Gi

---
# 3. 在 Pod 中使用
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
spec:
  containers:
  - name: app
    image: myapp:latest
    volumeMounts:
    - name: data
      mountPath: /data
  volumes:
  - name: data
    persistentVolumeClaim:
      claimName: my-pvc
```

---

### Q7: PVC 一直处于 Pending 状态？

**A:** 检查以下问题：

```bash
# 1. 检查 PVC 状态
kubectl describe pvc <pvc-name> -n <namespace>

# 2. 检查 StorageClass 是否存在
kubectl get storageclass

# 3. 检查 PV 状态
kubectl get pv

# 4. 检查 provisioner 日志
kubectl logs -n kube-system -l app=<provisioner-app>
```

---

## 应用部署

### Q8: 如何使用 Helm 部署应用？

**A:** 参考 `helm` 文档。基本步骤：

```bash
# 1. 安装 Helm
curl https://raw.githubusercontent.com/helm/helm/main/scripts/get-helm-3 | bash

# 2. 添加 Helm 仓库
helm repo add stable https://charts.helm.sh/stable
helm repo update

# 3. 搜索应用
helm search repo nginx

# 4. 安装应用
helm install my-nginx stable/nginx-ingress -n default

# 5. 查看部署
helm list -n default
helm status my-nginx -n default

# 6. 卸载应用
helm uninstall my-nginx -n default
```

详见: [helm](./helm)

---

### Q9: 使用 Ansible 自动化部署失败？

**A:** 检查 Ansible 配置：

```bash
# 1. 验证 Ansible 安装
ansible --version

# 2. 检查 inventory 文件
cat inventory.ini

# 3. 测试连接
ansible all -i inventory.ini -m ping

# 4. 运行 playbook 并查看详细输出
ansible-playbook -i inventory.ini playbook.yml -vvv

# 5. 常见问题
# - SSH 密钥权限: chmod 600 ~/.ssh/id_rsa
# - Python 环境: 确保远程主机安装了 Python
# - 网络连通性: ping <host-ip>
```

详见: [Ansible](./Ansible)

---

## 监控与日志

### Q10: 如何部署 Prometheus + Grafana 监控？

**A:** 参考 `Prometheus + Grafana` 文档。基本步骤：

```bash
# 1. 使用 Helm 安装
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts
helm install prometheus prometheus-community/kube-prometheus-stack

# 2. 访问 Grafana
kubectl port-forward svc/prometheus-grafana 3000:80
# 地址: http://localhost:3000
# 默认用户: admin
# 默认密码: prom-operator

# 3. 访问 Prometheus
kubectl port-forward svc/prometheus-kube-prometheus-prometheus 9090:9090
# 地址: http://localhost:9090
```

详见: [Prometheus + Grafana](./Prometheus%20%2B%20Grafana)

---

### Q11: 如何查看应用日志？

**A:** 使用 kubectl logs 命令。

```bash
# 1. 查看单个 Pod 日志
kubectl logs <pod-name> -n <namespace>

# 2. 查看实时日志
kubectl logs -f <pod-name> -n <namespace>

# 3. 查看前一个容器实例的日志 (Pod 重启后)
kubectl logs <pod-name> --previous -n <namespace>

# 4. 查看特定容器的日志
kubectl logs <pod-name> -c <container-name> -n <namespace>

# 5. 导出日志到文件
kubectl logs <pod-name> -n <namespace> > app.log

# 6. 查看多个 Pod 日志
kubectl logs -f deployment/<deployment-name> -n <namespace>
```

---

## 故障排除

### Q12: Pod 处于 CrashLoopBackOff 状态？

**A:** 这表示应用反复崩溃。检查步骤：

```bash
# 1. 查看 Pod 状态和事件
kubectl describe pod <pod-name> -n <namespace>

# 2. 查看应用日志
kubectl logs <pod-name> -n <namespace>
kubectl logs <pod-name> --previous -n <namespace>  # 查看崩溃前的日志

# 3. 常见原因
# - 镜像不存在: 检查镜像名称和 tag
# - 资源不足: 检查内存和 CPU 限制
# - 启动命令错误: 查看 Dockerfile 或 deployment 定义
# - 依赖服务不可用: 检查数据库、缓存等连接

# 4. 执行 shell 进入容器进行排查
kubectl exec -it <pod-name> -n <namespace> -- /bin/bash

# 5. 检查镜像日志
kubectl describe node <node-name>  # 查看节点上的镜像
docker logs <container-id>  # 在节点上查看容器日志
```

---

### Q13: 如何清理集群资源？

**A:** 安全的清理方法：

```bash
# 1. 删除命名空间 (会删除该命名空间的所有资源)
kubectl delete namespace <namespace-name>

# 2. 删除特定资源
kubectl delete pod <pod-name> -n <namespace>
kubectl delete deployment <deployment-name> -n <namespace>
kubectl delete service <service-name> -n <namespace>

# 3. 删除所有资源 (谨慎使用)
kubectl delete all --all -n <namespace>

# 4. 强制删除卡住的 Pod
kubectl delete pod <pod-name> -n <namespace> --grace-period=0 --force

# 5. 清理 PVC 和 PV
kubectl delete pvc <pvc-name> -n <namespace>
kubectl delete pv <pv-name>
```

---

### Q14: 集群磁盘空间不足？

**A:** 清理镜像和容器：

```bash
# 1. 检查磁盘使用情况
df -h
docker system df

# 2. 删除未使用的镜像
docker image prune -a

# 3. 删除未使用的容器
docker container prune

# 4. 删除未使用的卷
docker volume prune

# 5. 清理 kubelet 数据
kubectl drain <node-name> --ignore-daemonsets
# 在节点上执行: rm -rf /var/lib/kubelet/pods/*
kubectl uncordon <node-name>
```

---

### Q15: 如何备份和恢复集群？

**A:** 使用 ETCD 备份恢复：

```bash
# 1. 备份 ETCD
bash scripts/backup-etcd.sh

# 2. 恢复 ETCD (集群停机)
kubeadm reset
sudo ETCDCTL_API=3 etcdctl \
  --endpoints=127.0.0.1:2379 \
  --cacert=/etc/kubernetes/pki/etcd/ca.crt \
  --cert=/etc/kubernetes/pki/etcd/server.crt \
  --key=/etc/kubernetes/pki/etcd/server.key \
  snapshot restore /path/to/backup.db \
  --data-dir=/var/lib/etcd-restored

# 3. 启动集群
kubeadm init --ignore-preflight-errors=all
```

---

## 📚 相关资源

- [Kubernetes 官方文档](https://kubernetes.io/docs/)
- [kubectl 参考](https://kubernetes.io/docs/reference/kubectl/)
- [Helm 文档](https://helm.sh/docs/)
- [ETCD 官方文档](https://etcd.io/docs/)

