# Kubernetes Helm 
基础组件：

- Kubernetes v1.30
- containerd v2.2.1
- Helm v3
- Ingress-Nginx

---

# Helm 应用管理

## 1 Helm 简介

Helm 是 Kubernetes 的包管理工具，通过 Chart 模板化管理 Kubernetes 应用。

相比传统 YAML 部署方式，Helm 提供：

- 应用模板化管理
- 参数配置
- 版本控制
- 应用升级
- 快速回滚


Helm 核心组成：

```
Chart
 |
 ├── Chart.yaml
 |
 ├── values.yaml
 |
 └── templates
        |
        ├── deployment.yaml
        ├── service.yaml
        └── ingress.yaml
```

---
创建 Chart

执行：

```bash
helm create nginx
```

生成目录：

```
nginx
├── Chart.yaml
├── values.yaml
└── templates
    ├── deployment.yaml
    ├── service.yaml
    ├── ingress.yaml
    └── _helpers.tpl
```

---

#Helm 参数化配置
 values.yaml

通过 values.yaml 管理应用参数。


例如：

```yaml
replicaCount: 2
```


deployment.yaml：

```yaml
spec:
  replicas: {{ .Values.replicaCount }}
```


Helm 渲染后：

```yaml
replicas: 2
```


通过模板变量，实现 Kubernetes 资源动态配置。


---

# Helm 安装应用

# 安装 nginx

执行：

```bash
helm install my-nginx ./nginx
```


查看 Helm Release：

```bash
helm list
```


输出：

```
NAME        STATUS
my-nginx    deployed
```


查看 Kubernetes：

```bash
kubectl get deployment
```


结果：

```
NAME        READY
my-nginx    2/2
```


说明 Helm 已成功创建 Kubernetes 应用。


---

# Helm Upgrade 应用升级


修改：

```yaml
replicaCount: 5
```


执行：

```bash
helm upgrade my-nginx ./nginx
```


查看：

```bash
kubectl get deployment
```


结果：

```
NAME        READY
my-nginx    5/5
```


说明 Helm 可以通过修改 values.yaml 实现应用升级。

---

# Helm 版本管理与回滚


查看历史版本：

```bash
helm history my-nginx
```


示例：

```
REVISION    STATUS

1           superseded
2           superseded
3           deployed
```


回滚：

```bash
helm rollback my-nginx 1
```


验证：

```bash
helm history my-nginx
```


Helm 支持：

- 发布版本管理
- 快速恢复
- 应用生命周期管理


---

#Helm 部署问题排查


## 8.1 ImagePullBackOff


问题：

```
ImagePullBackOff
```


排查：

```bash
kubectl describe pod <pod-name>
```


查看 Events。


常见原因：

- 镜像不存在
- 镜像地址错误
- 私有仓库认证失败
- 节点无法访问镜像仓库


---

# Harbor 私有镜像接入实践


##  Docker 推送镜像


登录 Harbor：

```bash
docker login 192.168.76.10:80
```


推送：

```bash
docker push \
192.168.76.10:80/cloud-native/nginx-demo:v1
```


Docker 环境推送成功。


---

## Kubernetes 拉取问题


Kubernetes 使用 containerd 作为运行时。


出现：

```
ImagePullBackOff
```


查看 Pod：

```bash
kubectl describe pod
```


发现：

```
server gave HTTP response to HTTPS client
```


原因：

Harbor 使用：

```
http://192.168.76.10:80
```


containerd 默认使用：

```
https://192.168.76.10:80
```


导致协议不匹配。


---

##  containerd 排查过程


检查 containerd：

```bash
containerd --version
```


查看 registry 配置：

```bash
grep config_path /etc/containerd/config.toml
```


配置：

```
/etc/containerd/certs.d
```


通过 hosts.toml 配置 HTTP Registry。


最终验证：

```bash
ctr -n k8s.io images pull \
192.168.76.10:80/cloud-native/nginx-demo:v1
```


验证 containerd 是否可以访问 Harbor。


 Kubernetes 私有镜像仓库接入
# 私有镜像仓库接入

## 一、镜像拉取鉴权（imagePullSecrets）

为了使 Kubernetes 能够从 Harbor 私有镜像仓库拉取业务镜像，需要在集群中创建镜像拉取凭据（Secret）。

Secret 类型：

```text
kubernetes.io/dockerconfigjson
```

主要用于保存 Harbor 仓库的账号密码，在 Pod 拉取私有镜像时自动完成身份认证。

### 创建 Secret

```bash
kubectl create secret docker-registry harbor-secret \
  --docker-server=192.168.76.10:80 \
  --docker-username=admin \
  --docker-password=<password>


# 二、业务镜像发布流程

完成了基于 Harbor 私有仓库的业务镜像发布与部署流程，整体过程如下：

```text
Docker Build
      │
      ▼
Docker Tag
      │
      ▼
Docker Push
      │
      ▼
Harbor 私有仓库
      │
      ▼
Kubernetes Deployment
      │
      ▼
imagePullSecrets 鉴权
      │
      ▼
containerd 拉取镜像
      │
      ▼
Pod 成功运行
```

---
