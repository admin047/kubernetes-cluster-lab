# Prometheus + Grafana 监控平台搭建

---

# 一、添加 Helm 仓库

```bash
helm repo add prometheus-community https://prometheus-community.github.io/helm-charts

helm repo update
```

查看仓库：

```bash
helm repo list
```

---

# 二、创建监控命名空间

```bash
kubectl create namespace monitoring
```

查看：

```bash
kubectl get ns
```

---

# 三、安装 kube-prometheus-stack

安装监控组件：

```bash
helm install monitoring prometheus-community/kube-prometheus-stack \
-n monitoring
```

查看 Release：

```bash
helm list -n monitoring
```

---

# 四、验证组件

查看 Pod：

```bash
kubectl get pods -n monitoring
```

正常情况下可以看到：

* Prometheus
* Grafana
* Alertmanager
* node-exporter
* kube-state-metrics

查看 Service：

```bash
kubectl get svc -n monitoring
```

---

# 五、Grafana 配置

## 获取管理员账号

```bash
kubectl get secret monitoring-grafana \
-n monitoring \
-o jsonpath="{.data.admin-user}" | base64 -d
```

默认账号：

```text
admin
```

获取密码：

```bash
kubectl get secret monitoring-grafana \
-n monitoring \
-o jsonpath="{.data.admin-password}" | base64 -d
```

---

## 修改 Service 类型

默认 Grafana 使用 **ClusterIP**，仅支持集群内部访问。

为了方便实验，将 Service 修改为 **NodePort**。

```bash
kubectl patch svc monitoring-grafana \
-n monitoring \
-p '{"spec":{"type":"NodePort"}}'
```

查看端口：

```bash
kubectl get svc monitoring-grafana -n monitoring
```

浏览器访问：

```text
http://<NodeIP>:<NodePort>
```

登录 Grafana。

---

# 六、Prometheus 数据源验证

登录 Grafana 后，进入：

```text
Connections
    ↓
Data Sources
```

确认 Prometheus 数据源状态为：

```text
Data source is working
```

说明 Grafana 已成功连接 Prometheus。

---

# 七、导入 Dashboard

Grafana 支持导入官方 Dashboard。

推荐导入：

| Dashboard          | ID    |
| ------------------ | ----- |
| Node Exporter Full | 1860  |
| Kubernetes Cluster | 15757 |
| Kubernetes Node    | 15759 |
| Kubernetes Pod     | 15760 |

导入完成后，可以实时查看：

* 节点 CPU 使用率
* 节点内存使用率
* 磁盘使用率
* 网络流量
* Pod CPU / Memory
* Deployment 状态
* Namespace 资源使用情况

---

# 八、故障排查

## 1. Pod 长时间处于 ContainerCreating

查看 Pod：

```bash
kubectl describe pod <Pod名称> -n monitoring
```

发现：

```text
FailedMount
failed to sync configmap cache
```

继续查看 kubelet 日志：

```bash
journalctl -u kubelet
```

日志显示：

```text
grpc: addrConn.createTransport failed to connect

rpc error: code = Unavailable
```

原因：

kubelet 无法连接 containerd，导致 Pod 初始化失败。

处理方法：

```bash
systemctl restart containerd

systemctl restart kubelet
```

组件恢复正常运行。

---

## 2. Grafana 无法访问

原因：

Grafana Service 默认类型为：

```text
ClusterIP
```

解决：

修改为：

```text
NodePort
```

之后即可通过浏览器访问。

---



